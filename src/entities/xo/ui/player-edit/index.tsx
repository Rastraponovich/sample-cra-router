import { ControlledModal } from "shared/ui/modal"

export const PlayerEdit = () => {
    const onClose = () => {}

    const opened = true
    return (
        <ControlledModal opened={opened} onClose={onClose} title="edit player">
            <div>asdsadsa</div>
        </ControlledModal>
    )
}
